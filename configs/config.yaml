defaults:
  - data: abc-pc
  - model: dgcnn-4k
  - transforms: pc-basic
  - meta_arch: point-sharpness-regressor
  - trainer: trainer
  - scheduler: step
  - opt: adam

data_loader:
  train:
    total_batch_size: 16
    num_workers: 0  # depending on RAM capacity set this value carefully
  val:
    total_batch_size: 16
    num_workers: 0  # depending on RAM capacity set this value carefully
  test:
    total_batch_size: 16
    num_workers: 0  # depending on RAM capacity set this value carefully

eval_only: false
weights: null
seed: 123
