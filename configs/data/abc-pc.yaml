# @package _group_

dataset:
  resolution: high
  size: 64k
  data_dir: ${hydra:runtime.cwd}/data/abc/points/${data.dataset.resolution}/${data.dataset.size}
  filenames: null
  data_label: points
  target_label: distances
  max_loaded_files: 5 # TODO maybe try do it environmental variable?

train_dataset: ${data.dataset}
val_dataset: ${data.dataset}
test_dataset: ${data.dataset}
