# @package _group_

pl_class:
  _target_: sharpf.modeling.SharpFeaturesRegressionTask

loss:
  _target_: sharpf.modeling.kl_div_loss
  a: ${model._class.a}
  b: ${model._class.b}
  discretization: ${model._class.discretization}
  margin: ${model._class.margin}

checkpoint_on: mean_rmse/val
early_stop:
  value: ${task.checkpoint_on}
  mode: min
  min_delta: 0.001
  patience: 5
